export function renderTaskDetail() {

    console.log(
        "task detail loaded"
    );

    /*
    今後実装予定:

    - task詳細表示
    - task編集
    - subtask表示

    backend:
    GET /api/tasks/:id
    PUT /api/tasks/:id
    */
}